const depthFirstPrint = (graph, source) => {
    const stack = [source];

    while(stack = null){

    };

    const graph = {
        a: ['6','c'],
        b: ['d'],
        c: ['e'],
        d: ['f'],
        e: [],
        f: []
    }

    depthFirstPrint(graph, 'a');
}