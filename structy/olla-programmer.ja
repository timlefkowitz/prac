const greet = (s) => {
  // todo
    return "olla " + s;
};

console.log(greet("peanut"));
module.exports = {
  greet,
};


const maxValue = (nums) => {
  // todo
  let maximum = -Infinity;
  for(let num of nums){
    if(num > maximum){
      maximum = num;
    }
  }
  return maximum;
};

module.exports = {
  maxValue,
};